# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Secrets — never commit the real key (copy .env.example -> .env)
.env

# Simulink build artifacts
slprj/
*.slxc
*.autosave

# Agent-exported figures (watched + streamed inline, not source)
figures/

# Backend launch logs from run-demo.ps1
.run/

# Rehearsal screen recordings are large binaries — link them, don't commit them
# (see docs/rehearsal/fallback-assets.md). Screenshots (PNG) ARE committed.
*.mp4
*.mov
*.mkv
*.webm
